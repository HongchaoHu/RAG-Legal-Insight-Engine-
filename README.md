# RAG-Legal-Insight-Engine-
A Python RAG system that loads thousands of legal text files, creates concise LLM‑generated summaries, embeds them and stores the vectors in a FAISS index. It exposes a FastAPI endpoint that returns relevant answers with source references for any legal query. The codebase is modular, testable and ready to showcase on a résumé.
